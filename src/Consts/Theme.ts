import { createMuiTheme } from "@material-ui/core";

export const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#EF6C00",
      contrastText: "#fff",
    },
    secondary: {
      main: "#7F63F4",
    },
    background: {
      paper: "#FFF",
    },
  },
});
