/**
 * Конфигурация пункта меню.
 *
 * @prop {string} label Лейбл пункта меню.
 * @prop {JSX.Element} icon Иконка пункта.
 */
export interface IMenuItemConfig {
  label: string;
  icon: JSX.Element;
}
